{{/* <!--
	
	f/getblockscss

	get critical css resources matching list of "body" blocks
	
	Arguments:
	- anything (argument not used, but one must be passed)
	
	Process:
	- Checks for each "body" block if a .css file exists with same name
	  If yes, add it to the list of .css files
	- Then uses each part of the file name to include additional css
	  For example "pages-news" could include css from pages.scss and news.scss
	Returns:
	- file list as a list of strings (WITHOUT path, subfolder or extension)
	
 --> */}}

{{ $blocks := partial "f/getblocks" "body" }}
{{ $result := slice }}

{{ range $blocks }}
	{{ $filepath := printf "assets/css/%s.scss" . }}
	{{ if fileExists $filepath }}
		{{ $result = $result | append . }}
	{{ end }}
	{{ $split := strings.Split . "-" }}
	{{ $len := $split | len }}
	{{ if ge $len 2 }}
		{{ range $split }}
			{{ $filepathsplit := printf "assets/css/%s.scss" . }}
			{{ if fileExists $filepathsplit }}
				{{ $result = $result | append . }}
			{{ end }}	
		{{ end }}
	{{ end }}
{{ end }}

{{ return $result }}
