{{ $data := "" }}
{{ if . }}
	{{ $data = index hugo.Data.schema.organizations . }}
{{ else }}
	{{ $data = index hugo.Data.schema.organizations "ideasonstage" }}
{{ end }}

{ "@type": "{{ $data.type }}", "name": "{{ $data.name }}", "url": "{{ $data.url }}" }