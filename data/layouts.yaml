# /data/layouts.yaml is where all page structures are defined.
# this is a lot easier to read and maintain vs. multiple templates per page type
# head: blocks added to the <head></head> part
# body: blocks added to the <body></body> part
# side: blocks added to the sidebar inside the cl-content part
# main: blocks added inside the cl-content--main part. Use only if absolutely necessary
# list: blocks added inside list elements
# 
# Simple example without side or main blocks:
#   <head>
#       _common "head" blocks
#       specific "head" blocks
#   </head>
#   <body>
#       _common "bodytop" blocks
#       specific "body" blocks
#       _common "bodybottom" blocks
#   </body>
#   <footer>
#       _common "footer" blocks
#   </footer>
# 
# Example of body with side and cont blocks:
#   <body>
#       _common "bodytop" blocks
#       "body" blocks, and inside the body block named "content" we have the following div:
#       <div class="cl-content">
#           <div class="cl-content--sidebar">
#               "side" blocks
#           </div>
#           <div class="cl-content--main">
#               "main" blocks
#           </div>
#       </div>
#       _common "bodybottom" blocks
#   </body>



# common blocks are added to ALL pages
# they cannot be overridden
_common:
    # common header blocks for all pages
    head:
        - css
        - meta
        - hreflang
        - alternate
        - favicon
        - schema
    # common body blocks above "main" for all pages
    bodytop:
        - navbar
    # do not add body: to _common, use bodytop: or bodybottom: instead
    # common body blocks after "main" for all pages
    bodybottom:
        - contact
    # common footer blocks for all pages
    footer:
        - footer



# default blocks are used if no specific blocks are defined for a page type.
# they can be overridden by a specific .Type and .Kind combination below in this file
# or by a "body:" argument followed by a list (similar to the body: declaration this file) in the front matter of a page
_default:
    # page.Kind "section"
    section:
        head:
            - meta-desc-section
            - title-section
        body:
            - h1hero
            - breadcrumbs
            - content
            - pages
            - modules-aside
        main:
            - contentmain
    # page.Kind "single"
    page:
        head:
            - meta-desc-single
            - title-single
        body:
            - h1hero
            - breadcrumbs
            - content
            - modules-aside
        main:
            - contentmain
    # page.Kind "home"
    home:
        head:
            - meta-desc-single
            - schema-corporation
            - schema-website
            - title-home
        body:
            - h1home
            - countrylist
            - cta
            - contentwide
            - references
            - modules-aside
            - testimonials
            - location
        main:
            - modules
            - contentshort


# page.Type "404"
404:
    # page.Kind "page"
    page:
        head:
            - meta-desc-single
            - title-404
        body:
            - content404



# below are the specific .Type + .Kind combinations that can override the _default blocks.
# they can be overridden for a page by a "body:" argument in the page front matter
# to know the exact .Type and .Kind of a page, temporarily put {{ .Type }} {{ .Kind }} in the body of baseof.html template

# page.Type "news"
news:
    # page.Kind "section"
    section:
        # each part defined below overrides default for page Type "news" Kind "section"
        # here, there is no "head:" part defined so _default:section:head: will be used
        # body part is defined so it will override _default:section:body: part
        body:
            - h1hero
            - content
        # content main blocks
        # will appear where the "content" item is inserted in "body:"
        # if there is no "content" item it will not be used
        main:
            - list
            - buttonprevnextnews
        # content sidebar blocks
        # will appear where the "content" item is inserted in "body:" (there must be a content item in body:)
        # if there is no "content" item it will not be used
        side:
            - topcategories
        # list of pages blocks (repeated for each page listed)
        # will appear where the "list" item is inserted in "body:" or "main:"
        # if there is no "list" item it will not be used
        list:
            - title
            - metadata
            - tags
            - banner
            - summary
            - buttonmorenews
            - space-medium
    page:
        head:
            - meta-desc-news-single
            - title-single
        body:
            - breadcrumbs
            - content
        side:
            - related
            - topcategories
        main:
            - h1hero
            - metadata
            - tags
            - contentmain


# page.Type "references"
references:
    section:
        body:
            - h1hero
            - references
            - content
        main:
            - pages-references


# page.Type "revolution"
revolution:
    section:
        head:
            - meta-desc-section
            - title-section
        body:
            - h1hero
            - content-revolution-section
    page:
        body:
            - h1hero
            - breadcrumbs
            - content-revolution-single



# page.Type "tags"
tags:
    # taxonomy = section for tags
    taxonomy:
        head:
            - meta-desc-tags-taxonomy
            - meta-robots-ni
            - title-tags-taxonomy
        body:
            - h1hero
            - content
            - pages-tags
    # term = page for tags
    term:
        head:
            - meta-desc-tags-term
            - meta-robots-ni
            - title-tags-term
        body:
            - h1hero
            - breadcrumbs
            - content
            - pages-tags



# page.Type "team"
team:
    section:
        head:
            - meta-desc-section
            - title-section
        body:
            - h1hero
            - contentwide
        main:
            - contentshort
            - team
    page:
        head:
            - meta-desc-team-single
            - schema-person
            - title-team-single
        body:
            - breadcrumbs
            - content
        main:
            - h1team
            - positionteam
            - contentmain
        side:
            - phototeam



# page.Type "training"
training:
    section:
        body:
            - h1hero
            - breadcrumbs
            - references
            - content
            - pages
        side:
            - links
    page:
        body:
            - h1hero
            - content
        side:
            - training
